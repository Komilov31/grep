Первый тестовый текст для проверки grep.

Это дополнительная строка без искомого слова.
Еще одна строка для проверки.

Здесь есть слово grep несколько раз.

Строка без совпадений совсем.
И ещё одна пустая по grep.

grep встречается в первой строке.

Ни слова grep здесь.
Просто обычный текст.

А вот в третьей строке также есть слово grep.

Тестируем разные случаи.
И добавляем строку без совпадений.

Повторяем слово grep для тестирования.

Эти строки не имеют grep.
И это чистый текст.

grep можно искать в любом регистре.

Здесь нет нужного слова.
Просто текст для проверки.

Например, Grep, GReP или GREP.

Строка без слова.
И ещё одна строка.

В этих строках есть разные формы слова grep.

Поддерживаем тесты.
Добавляем пустые строки.

Также проверим поиск слова "текст".

Проверочные данные в тексте.
Без слова grep.

Этот текст содержит слово ТЕКСТ с разными регистрами.

Дополнительные строки.
Для проверки работы.

Если поиск чувствителен к регистру, то найти ТЕКСТ и текст будет по-разному.

Тест без grep.
И ещё одна строка.

Проверим строки без слова grep.

Пара строк без grep.
И следующая тоже.

В этой строке нет искомого слова.

Добавим строки.
Без искомого слова.

Еще одна строка без совпадений.

Пустые строки.
Текст простой.

Иногда слово может быть частью другого слова, например grepping.

Но здесь нет clean text.
И это важно.

Поиск должен корректно работать с такими случаями.

Строка без совпадений.
Ещё одна строка.

Проверим строки с числами: grep123 и 123grep.

Текст без grep.
Чистая строка.

grep! Восклицательный знак рядом со словом.

Пара строк без grep.
И ещё одна.

В конце повторим слово grep ещё раз.

Заключительные строки.
Просто тест.